#include<stdio.h>

int main() {
    int a = 20;
    int b = 20;

    // Relational Operator: ==, >, >=, <, <=, !=

    if(a != b) {
        printf("True");
    }
    else {
        printf("False");
    }
    return 0;
}